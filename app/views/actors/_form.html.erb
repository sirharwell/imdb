<h1><%= @actor.id ? 'Edit Actor' : 'Create Actor' %></h1>
<hr />
<%= form_for [@film, @actor] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <br />
  <%= f.label :body %>
  <%= f.text_area :body, required: true %>
  <%= f.submit %>
<% end %>
